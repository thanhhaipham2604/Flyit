"""Clean Markdown while preserving meaning.

Headings, lists and tables must stay intact - chunk boundaries and citations
depend on them. Strip boilerplate/navigation junk, normalise whitespace.
"""


def clean(text: str) -> str:
    """TODO: strip boilerplate, normalise, preserve headings/lists/tables."""
    raise NotImplementedError
